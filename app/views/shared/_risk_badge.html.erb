<% level = local_assigns[:level] || "safe" %>
<% score = local_assigns[:score] %>
<span class="badge-<%= level %>">
  <% if score %>
    <%= score %>/100 · <%= level.capitalize %>
  <% else %>
    <%= level.capitalize %>
  <% end %>
</span>
